[project]
name = "github-actions-learning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pytest>=8.4.1",
]

[dependency-groups]
dev = [
    "rich>=14.1.0",
]
